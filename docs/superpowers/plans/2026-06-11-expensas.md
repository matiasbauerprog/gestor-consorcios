# Módulo de Expensas y Comprobantes — Plan de Implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implementar el módulo de Expensas y Comprobantes end-to-end: 4 cambios en el backend (nuevo `GET /comprobantes`, `ExpensaOut.ultimo_comprobante`, `GET /expensas?departamento_id=`, `ComprobanteOut.expensa` resumen) + dos pantallas frontend (`/expensas` con creación, presentación y aprobación de comprobantes; `/comprobantes` con filtros y acciones inline).

**Architecture:** Backend FastAPI suma una relationship `Expensa↔Comprobante` para soportar embedding bidireccional, un endpoint `GET /comprobantes` con aislamiento por rol, y un query param `departamento_id` en `/expensas`. Frontend agrega dos rutas y dos pantallas que reutilizan el shell + `Modal` + `Tarjeta` ya existentes; suma `BadgeEstado` y `SelectorDepartamento` como componentes reutilizables. CSS mobile-first siguiendo la regla del proyecto.

**Tech Stack:**
- Backend: Python 3.11+, FastAPI, SQLAlchemy 2.0, SQLite, pytest.
- Frontend: React 19, Vite 8, react-router-dom v6 (ya instalado).
- Tests backend: `./.venv/Scripts/python.exe -m pytest -v`.
- Servidor dev backend: `uvicorn backend.main:app --reload`.
- Servidor dev frontend: `npm run dev` desde `frontend/`.

**Notas previas para el implementador:**
- El proyecto es un repo git (rama `master`). Antes de empezar la implementación, crear `feature/expensas-frontend` y trabajar ahí. Cada task termina con un commit.
- Reglas del proyecto a respetar:
  - `openapi-first.md`: documentar endpoints en `openapi.yaml` ANTES de implementarlos.
  - `backend.md`: routers en `backend/routers/`, modelos en `backend/models.py`, schemas en `backend/schemas.py`.
  - `frontend.md`: HTML semántico, paleta CSS variables, **mobile-first** (base ≥320px; `@media (min-width: 600px)` y `@media (min-width: 960px)`).
  - `security.md`: identidad y rol vienen del JWT, nunca del body/query.
- La paleta CSS en `frontend/src/index.css` ya está. Solo sumamos 2 variables nuevas para "pendiente de verificación" (`--color-warning`, `--color-warning-bg`).
- Existen ya estos componentes/módulos en frontend: `Modal`, `Tarjeta`, `RequireAuth`, `AppLayout`, `Sidebar`, `apiFetch`. No volverlos a crear.

---

## Setup inicial: rama de trabajo

Antes de la Task 1, ejecutar:

```bash
git checkout -b feature/expensas-frontend
```

Trabajar todo en esta rama. El merge a `master` se hace al final, fuera del scope del plan.

---

## Fase A — Backend: 4 cambios precondicionales

### Task 1: Documentar los 4 cambios en `openapi.yaml`

**Files:**
- Modify: `openapi.yaml`

- [ ] **Step 1: Documentar el nuevo `GET /comprobantes`**

Insertar este bloque en `openapi.yaml` **antes** de la sección `# 5. AMENITIES` (o donde corresponda — busca la sección actual de `/comprobantes/{comprobante_id}` que existe para el PATCH, y agregá esta entrada como otra path key):

```yaml
  /comprobantes:
    get:
      tags: [Expensas]
      summary: Listar comprobantes
      description: |
        Devuelve comprobantes. Administración ve todos los del consorcio;
        Departamento ve sólo los de sus propias expensas (filtrado server-side).
        Representante no tiene acceso.
      operationId: listarComprobantes
      parameters:
        - name: estado
          in: query
          required: false
          description: Filtrar por estado del comprobante.
          schema:
            $ref: '#/components/schemas/EstadoComprobante'
        - name: departamento_id
          in: query
          required: false
          description: Filtrar por departamento (Admin). Ignorado para Departamento.
          schema:
            type: integer
            format: int64
            minimum: 1
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 50
        - name: offset
          in: query
          required: false
          schema:
            type: integer
            minimum: 0
            default: 0
      responses:
        '200':
          description: Lista de comprobantes.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Comprobante'
        '401':
          $ref: '#/components/responses/NoAutenticado'
        '403':
          $ref: '#/components/responses/AccesoDenegado'
```

- [ ] **Step 2: Documentar el query param `departamento_id` en `listarExpensas`**

Buscar la operación `listarExpensas` en `openapi.yaml` (path `/expensas`, verbo `get`). En su array `parameters`, agregar:

```yaml
        - name: departamento_id
          in: query
          required: false
          description: |
            Filtrar por departamento. Sólo efectivo para Administración;
            los usuarios Departamento siempre ven solo sus propias expensas.
          schema:
            type: integer
            format: int64
            minimum: 1
```

- [ ] **Step 3: Agregar `ultimo_comprobante` al componente `Expensa`**

Buscar `components > schemas > Expensa` en `openapi.yaml`. Sumar al `properties` (y al final del bloque, no en required):

```yaml
        ultimo_comprobante:
          description: Comprobante más reciente asociado, o null si no hay.
          oneOf:
            - $ref: '#/components/schemas/Comprobante'
            - type: 'null'
```

- [ ] **Step 4: Agregar `expensa` al componente `Comprobante`**

Buscar `components > schemas > Comprobante`. Sumar al `properties`:

```yaml
        expensa:
          description: Resumen de la expensa asociada (departamento, período, monto).
          oneOf:
            - $ref: '#/components/schemas/ExpensaResumen'
            - type: 'null'
```

Y definir el nuevo componente `ExpensaResumen` debajo de `Expensa`:

```yaml
    ExpensaResumen:
      type: object
      description: Resumen mínimo de una expensa para embeber en comprobantes.
      properties:
        departamento_id:
          type: integer
          format: int64
        periodo:
          type: string
          pattern: '^\d{4}-(0[1-9]|1[0-2])$'
        monto:
          type: number
          format: double
      required:
        - departamento_id
        - periodo
        - monto
```

- [ ] **Step 5: Validar YAML**

Run: `python -c "import yaml; yaml.safe_load(open('openapi.yaml', encoding='utf-8'))"`
Expected: sin salida (parsea bien).

- [ ] **Step 6: Commit**

```bash
git add openapi.yaml
git commit -m "docs(openapi): GET /comprobantes, expensa.ultimo_comprobante, comprobante.expensa, departamento_id en /expensas"
```

---

### Task 2: Models + Schemas — relationships y nuevos campos

**Files:**
- Modify: `backend/models.py` (clases `Expensa` y `Comprobante`)
- Modify: `backend/schemas.py` (nuevo `ExpensaResumen`, `ExpensaOut.ultimo_comprobante`, `ComprobanteOut.expensa`)

- [ ] **Step 1: Agregar relationships en `backend/models.py`**

Buscar la clase `Expensa` (alrededor de la línea 177). Sumar al final del cuerpo de la clase (antes de la siguiente clase):

```python
    comprobantes: Mapped[list["Comprobante"]] = relationship(
        back_populates="expensa",
        order_by="desc(Comprobante.fecha_creacion), desc(Comprobante.id)",
        lazy="selectin",
    )
```

Buscar la clase `Comprobante` (alrededor de la línea 202). Sumar al final del cuerpo:

```python
    expensa: Mapped["Expensa"] = relationship(back_populates="comprobantes")
```

- [ ] **Step 2: Verificar que el módulo importa sin errores**

Run: `./.venv/Scripts/python.exe -c "from backend.models import Expensa, Comprobante; e = Expensa.__table__; print('ok')"`
Expected: imprime `ok`.

- [ ] **Step 3: Agregar `ExpensaResumen` a `backend/schemas.py`**

Buscar la clase `ComprobanteOut` en `schemas.py`. Justo antes, insertar:

```python
class ExpensaResumen(BaseModel):
    model_config = ConfigDict(from_attributes=True)

    departamento_id: int
    periodo: str
    monto: float
```

- [ ] **Step 4: Sumar `expensa` a `ComprobanteOut`**

En `schemas.py`, buscar `ComprobanteOut`. Agregar al final de la clase:

```python
    expensa: ExpensaResumen | None = None
```

- [ ] **Step 5: Sumar `ultimo_comprobante` a `ExpensaOut`**

En `schemas.py`, buscar `ExpensaOut`. Agregar al final de la clase:

```python
    ultimo_comprobante: ComprobanteOut | None = None
```

> Nota: `ExpensaOut` define `ultimo_comprobante` referenciando `ComprobanteOut`. Si `ComprobanteOut` está definido más arriba que `ExpensaOut`, todo bien. Si no (mirar el orden), reordenar para que `ComprobanteOut` aparezca antes que `ExpensaOut` en el archivo, o usar forward refs `"ComprobanteOut"` con `model_rebuild()`. La forma más simple es reordenar.

- [ ] **Step 6: Verificar que los schemas cargan**

Run: `./.venv/Scripts/python.exe -c "from backend.schemas import ExpensaOut, ComprobanteOut, ExpensaResumen; print(ExpensaOut.model_fields.keys()); print(ComprobanteOut.model_fields.keys())"`
Expected: imprime las keys de cada schema, incluyendo `ultimo_comprobante` en ExpensaOut y `expensa` en ComprobanteOut.

- [ ] **Step 7: Correr la suite completa de tests para descartar regresiones**

Run: `./.venv/Scripts/python.exe -m pytest -v 2>&1 | tail -5`
Expected: todos los tests existentes siguen verdes (223 pasan).

- [ ] **Step 8: Commit**

```bash
git add backend/models.py backend/schemas.py
git commit -m "feat(models,schemas): relationship Expensa<->Comprobante, ExpensaResumen, ultimo_comprobante, expensa embebidos"
```

---

### Task 3 (TDD): `ExpensaOut.ultimo_comprobante` populated en `GET /expensas`

**Files:**
- Test: `tests/test_expensas.py` (sumar nuevos tests al final del bloque GET de listar)
- Modify (si hace falta): `backend/routers/expensas.py`

- [ ] **Step 1: Escribir tests que fallen**

Sumar al final de `tests/test_expensas.py`, antes del bloque de POST (o donde corresponda según el orden del archivo):

```python
# ---------------------------------------------------------------------------
# ExpensaOut.ultimo_comprobante
# ---------------------------------------------------------------------------


def test_listar_expensas_ultimo_comprobante_es_null_si_no_hay(client, headers_admin):
    r = client.get("/expensas", headers=headers_admin)
    assert r.status_code == 200
    for expensa in r.json():
        assert expensa["ultimo_comprobante"] is None


def test_listar_expensas_ultimo_comprobante_es_el_mas_reciente(
    client, headers_depto_a, db_session
):
    from datetime import date, datetime, timezone, timedelta
    from backend.models import Comprobante, EstadoComprobante

    # La expensa 100 pertenece al depto A (ver conftest).
    c_viejo = Comprobante(
        id=500,
        expensa_id=100,
        fecha_pago=date(2026, 5, 5),
        monto=50000.0,
        archivo_url=None,
        estado=EstadoComprobante.rechazado,
    )
    c_nuevo = Comprobante(
        id=501,
        expensa_id=100,
        fecha_pago=date(2026, 5, 6),
        monto=85000.0,
        archivo_url="https://drive.example/x.pdf",
        estado=EstadoComprobante.pendiente_verificacion,
    )
    db_session.add_all([c_viejo, c_nuevo])
    db_session.flush()
    # Forzar fecha_creacion: nuevo > viejo (en una segunda al menos).
    c_viejo.fecha_creacion = datetime.now(timezone.utc) - timedelta(minutes=10)
    c_nuevo.fecha_creacion = datetime.now(timezone.utc)
    db_session.commit()

    r = client.get("/expensas", headers=headers_depto_a)
    assert r.status_code == 200
    expensa_100 = next(e for e in r.json() if e["id"] == 100)
    assert expensa_100["ultimo_comprobante"] is not None
    assert expensa_100["ultimo_comprobante"]["id"] == 501
    assert expensa_100["ultimo_comprobante"]["estado"] == "pendiente_verificacion"
```

- [ ] **Step 2: Correr los tests nuevos y ver que fallan**

Run: `./.venv/Scripts/python.exe -m pytest tests/test_expensas.py -v -k ultimo_comprobante`
Expected: 2 tests, ambos FALLAN — el field `ultimo_comprobante` no aparece en la respuesta (o el endpoint sí lo devuelve si Pydantic ya lo serializa). Si los dos tests pasan al primer intento, significa que Pydantic + el relationship + `from_attributes=True` ya hicieron todo solos — no hace falta tocar el router. Si fallan, ir al Step 3.

- [ ] **Step 3 (condicional): Forzar el mapeo en el router**

Si los tests pasaron en Step 2, saltar a Step 4.

Si fallaron: editar `backend/routers/expensas.py`. Buscar `listar_expensas` y `obtener_expensa`. Cambiar el `return` para usar Pydantic explícitamente sobre cada entidad:

```python
# En listar_expensas, reemplazar:
return list(db.scalars(stmt).all())
# por:
expensas = list(db.scalars(stmt).all())
return [ExpensaOut.model_validate(e) for e in expensas]
```

```python
# En obtener_expensa, reemplazar:
return expensa
# por:
return ExpensaOut.model_validate(expensa)
```

(`ExpensaOut` debe estar importado en el archivo; verificar el `from ..schemas import ...` de arriba.)

- [ ] **Step 4: Correr los tests y confirmar que pasan**

Run: `./.venv/Scripts/python.exe -m pytest tests/test_expensas.py -v -k ultimo_comprobante`
Expected: 2 PASSED.

- [ ] **Step 5: Correr el archivo completo para descartar regresiones**

Run: `./.venv/Scripts/python.exe -m pytest tests/test_expensas.py -v`
Expected: todo verde.

- [ ] **Step 6: Commit**

```bash
git add tests/test_expensas.py backend/routers/expensas.py
git commit -m "feat(expensas): ExpensaOut incluye ultimo_comprobante (TDD)"
```

---

### Task 4 (TDD): `ComprobanteOut.expensa` populated en `POST /expensas/{id}/comprobantes`

**Files:**
- Test: `tests/test_expensas.py` o `tests/test_comprobantes.py` (donde haya tests del POST de presentar comprobante; verificar primero).

- [ ] **Step 1: Identificar el archivo correcto**

Run: `./.venv/Scripts/python.exe -m pytest tests/ -v --collect-only 2>&1 | grep -i "presentar\|comprobante" | head -10`
Expected: lista de tests existentes. Identificar el archivo donde están los tests del POST `presentar_comprobante` (probablemente `tests/test_expensas.py` o `tests/test_comprobantes.py`).

- [ ] **Step 2: Escribir el test que falla**

Sumar al final del archivo identificado:

```python
def test_presentar_comprobante_devuelve_expensa_resumen(client, headers_depto_a):
    payload = {
        "fecha_pago": "2026-06-05",
        "monto": 85000.00,
        "archivo_url": None,
    }
    r = client.post("/expensas/100/comprobantes", json=payload, headers=headers_depto_a)
    assert r.status_code == 201
    body = r.json()
    assert "expensa" in body
    assert body["expensa"] is not None
    assert body["expensa"]["departamento_id"] == 1
    assert body["expensa"]["periodo"] == "2026-05"
    assert body["expensa"]["monto"] == 85000.00
```

(Los valores `periodo` y `monto` de la expensa 100 vienen del seed en `tests/conftest.py`.)

- [ ] **Step 3: Correr el test y ver que falla**

Run: `./.venv/Scripts/python.exe -m pytest -v -k presentar_comprobante_devuelve_expensa_resumen`
Expected: FAIL (el field `expensa` falta).

- [ ] **Step 4: Verificar si pasa solo (sin tocar router)**

Si Pydantic ya popula `expensa` a partir del relationship (porque `model_config = ConfigDict(from_attributes=True)`), puede que el test pase sin más cambios. Verificá el test resultado del Step 3. Si pasa, saltar a Step 6.

- [ ] **Step 5 (condicional): Forzar el refresh en el router**

Si Step 3 falló: editar `backend/routers/expensas.py`, función `presentar_comprobante`. Después del `db.refresh(comprobante)`, sumar:

```python
    # Forzar que la relationship se cargue para que ComprobanteOut.expensa se popule.
    db.refresh(comprobante, attribute_names=["expensa"])
    return ComprobanteOut.model_validate(comprobante)
```

Asegurarse de que `ComprobanteOut` esté importado.

- [ ] **Step 6: Confirmar que pasa**

Run: `./.venv/Scripts/python.exe -m pytest -v -k presentar_comprobante_devuelve_expensa_resumen`
Expected: PASS.

- [ ] **Step 7: Suite completa**

Run: `./.venv/Scripts/python.exe -m pytest -v 2>&1 | tail -5`
Expected: todo verde.

- [ ] **Step 8: Commit**

```bash
git add tests/test_expensas.py backend/routers/expensas.py 2>/dev/null || git add tests/test_comprobantes.py backend/routers/expensas.py
git commit -m "feat(expensas): POST comprobante devuelve ComprobanteOut.expensa resumen (TDD)"
```

---

### Task 5 (TDD): `GET /expensas?departamento_id=` filtra para Admin

**Files:**
- Test: `tests/test_expensas.py`
- Modify: `backend/routers/expensas.py` (función `listar_expensas`)

- [ ] **Step 1: Escribir tests que fallen**

Sumar al final del bloque GET en `tests/test_expensas.py`:

```python
def test_listar_expensas_admin_filtra_por_departamento(client, headers_admin):
    r = client.get("/expensas?departamento_id=1", headers=headers_admin)
    assert r.status_code == 200
    expensas = r.json()
    assert len(expensas) > 0
    assert all(e["departamento_id"] == 1 for e in expensas)


def test_listar_expensas_admin_filtra_por_departamento_inexistente_devuelve_lista_vacia(
    client, headers_admin
):
    r = client.get("/expensas?departamento_id=99999", headers=headers_admin)
    assert r.status_code == 200
    assert r.json() == []


def test_listar_expensas_departamento_ignora_query_de_otro_depto(client, headers_depto_a):
    # Depto A intenta pedir las del depto B; el backend debe seguir devolviendo
    # solo las de A (ignorar el query param).
    r = client.get("/expensas?departamento_id=2", headers=headers_depto_a)
    assert r.status_code == 200
    expensas = r.json()
    assert all(e["departamento_id"] == 1 for e in expensas)
```

- [ ] **Step 2: Correr los tests y ver que fallan**

Run: `./.venv/Scripts/python.exe -m pytest tests/test_expensas.py -v -k "filtra_por_departamento or ignora_query"`
Expected: 3 tests fallan (`departamento_id` no es un parámetro válido → 422, o no filtra y devuelve todas).

- [ ] **Step 3: Implementar el filtro**

Editar `backend/routers/expensas.py`, función `listar_expensas`. La firma actual es:

```python
def listar_expensas(
    periodo: str | None = Query(...),
    estado: EstadoExpensa | None = Query(...),
    limit: int = Query(...),
    offset: int = Query(...),
    db: Session = Depends(get_db),
    user: CurrentUser = Depends(require_roles(Rol.administracion, Rol.departamento)),
) -> list[Expensa]:
```

Agregar el parámetro `departamento_id` después de `estado`:

```python
    departamento_id: int | None = Query(default=None, gt=0, description="Filtrar por depto (Admin)."),
```

Después del bloque `if user.rol == Rol.departamento: stmt = stmt.where(...)` que filtra por su depto, agregar:

```python
    # El query departamento_id solo es efectivo para Admin: el filtro de Depto
    # ya prevalece arriba (su token define qué ve).
    if user.rol != Rol.departamento and departamento_id is not None:
        stmt = stmt.where(Expensa.departamento_id == departamento_id)
```

- [ ] **Step 4: Confirmar que los tests pasan**

Run: `./.venv/Scripts/python.exe -m pytest tests/test_expensas.py -v -k "filtra_por_departamento or ignora_query"`
Expected: 3 PASSED.

- [ ] **Step 5: Suite completa**

Run: `./.venv/Scripts/python.exe -m pytest tests/test_expensas.py -v`
Expected: todo verde.

- [ ] **Step 6: Commit**

```bash
git add tests/test_expensas.py backend/routers/expensas.py
git commit -m "feat(expensas): GET /expensas acepta departamento_id (Admin) (TDD)"
```

---

### Task 6 (TDD): `GET /comprobantes` con auth, filtros y errores

**Files:**
- Test: `tests/test_comprobantes.py` (sumar al inicio del archivo, antes del bloque PATCH existente)
- Modify: `backend/routers/comprobantes.py` (router)

- [ ] **Step 1: Escribir todos los tests del nuevo endpoint**

Insertar al **principio** de `tests/test_comprobantes.py` (después de los imports si los hay, o al tope si no), antes del bloque PATCH:

```python
# ---------------------------------------------------------------------------
# GET /comprobantes
# ---------------------------------------------------------------------------


from datetime import date
from backend.models import Comprobante, EstadoComprobante


def _crear_comprobante(db, expensa_id, fecha_pago, monto, estado):
    c = Comprobante(
        expensa_id=expensa_id,
        fecha_pago=fecha_pago,
        monto=monto,
        archivo_url=None,
        estado=estado,
    )
    db.add(c)
    db.commit()
    db.refresh(c)
    return c


def test_listar_comprobantes_sin_token_devuelve_401(client):
    r = client.get("/comprobantes")
    assert r.status_code == 401


def test_listar_comprobantes_como_representante_devuelve_403(client, headers_representante):
    r = client.get("/comprobantes", headers=headers_representante)
    assert r.status_code == 403


def test_listar_comprobantes_admin_devuelve_todos(client, headers_admin, db_session):
    _crear_comprobante(db_session, 100, date(2026, 5, 5), 85000, EstadoComprobante.aprobado)
    _crear_comprobante(db_session, 101, date(2026, 5, 6), 92000, EstadoComprobante.pendiente_verificacion)

    r = client.get("/comprobantes", headers=headers_admin)
    assert r.status_code == 200
    assert len(r.json()) == 2


def test_listar_comprobantes_admin_filtra_por_estado(client, headers_admin, db_session):
    _crear_comprobante(db_session, 100, date(2026, 5, 5), 85000, EstadoComprobante.aprobado)
    _crear_comprobante(db_session, 101, date(2026, 5, 6), 92000, EstadoComprobante.pendiente_verificacion)

    r = client.get("/comprobantes?estado=aprobado", headers=headers_admin)
    assert r.status_code == 200
    comprobantes = r.json()
    assert len(comprobantes) == 1
    assert comprobantes[0]["estado"] == "aprobado"


def test_listar_comprobantes_admin_filtra_por_departamento(client, headers_admin, db_session):
    _crear_comprobante(db_session, 100, date(2026, 5, 5), 85000, EstadoComprobante.aprobado)  # depto 1
    _crear_comprobante(db_session, 101, date(2026, 5, 6), 92000, EstadoComprobante.pendiente_verificacion)  # depto 2

    r = client.get("/comprobantes?departamento_id=1", headers=headers_admin)
    assert r.status_code == 200
    comprobantes = r.json()
    assert len(comprobantes) == 1
    assert comprobantes[0]["expensa"]["departamento_id"] == 1


def test_listar_comprobantes_departamento_solo_ve_los_suyos(client, headers_depto_a, db_session):
    _crear_comprobante(db_session, 100, date(2026, 5, 5), 85000, EstadoComprobante.aprobado)  # depto 1
    _crear_comprobante(db_session, 101, date(2026, 5, 6), 92000, EstadoComprobante.pendiente_verificacion)  # depto 2

    r = client.get("/comprobantes", headers=headers_depto_a)
    assert r.status_code == 200
    comprobantes = r.json()
    assert len(comprobantes) == 1
    assert comprobantes[0]["expensa"]["departamento_id"] == 1


def test_listar_comprobantes_departamento_ignora_query_de_otro_depto(client, headers_depto_a, db_session):
    _crear_comprobante(db_session, 100, date(2026, 5, 5), 85000, EstadoComprobante.aprobado)  # depto 1
    _crear_comprobante(db_session, 101, date(2026, 5, 6), 92000, EstadoComprobante.pendiente_verificacion)  # depto 2

    r = client.get("/comprobantes?departamento_id=2", headers=headers_depto_a)
    assert r.status_code == 200
    comprobantes = r.json()
    # Sigue devolviendo solo los de su propio depto.
    assert all(c["expensa"]["departamento_id"] == 1 for c in comprobantes)
```

- [ ] **Step 2: Correr los tests y ver que fallan**

Run: `./.venv/Scripts/python.exe -m pytest tests/test_comprobantes.py -v -k listar_comprobantes`
Expected: todos los tests fallan (endpoint 405 Method Not Allowed o 404).

- [ ] **Step 3: Implementar el endpoint**

Editar `backend/routers/comprobantes.py`. Actualizar los imports al tope:

```python
from fastapi import APIRouter, Depends, HTTPException, Query, status
from sqlalchemy import select
from sqlalchemy.orm import Session

from ..auth import CurrentUser, require_roles
from ..database import get_db
from ..models import Comprobante, EstadoComprobante, EstadoExpensa, Expensa, Rol
from ..schemas import ComprobanteActualizar, ComprobanteOut
```

Agregar el endpoint GET **antes** del PATCH existente (mantener el PATCH intacto):

```python
@router.get(
    "",
    response_model=list[ComprobanteOut],
    status_code=status.HTTP_200_OK,
    summary="Listar comprobantes",
)
def listar_comprobantes(
    estado: EstadoComprobante | None = Query(default=None),
    departamento_id: int | None = Query(default=None, gt=0),
    limit: int = Query(default=50, ge=1, le=200),
    offset: int = Query(default=0, ge=0),
    db: Session = Depends(get_db),
    user: CurrentUser = Depends(require_roles(Rol.administracion, Rol.departamento)),
) -> list[Comprobante]:
    stmt = (
        select(Comprobante)
        .join(Expensa, Comprobante.expensa_id == Expensa.id)
        .order_by(Comprobante.fecha_creacion.desc(), Comprobante.id.desc())
    )

    # Aislamiento por unidad para Departamentos. Su token define qué pueden ver;
    # el query departamento_id es ignorado.
    if user.rol == Rol.departamento:
        stmt = stmt.where(Expensa.departamento_id == user.departamento_id)
    elif departamento_id is not None:
        stmt = stmt.where(Expensa.departamento_id == departamento_id)

    if estado is not None:
        stmt = stmt.where(Comprobante.estado == estado)

    stmt = stmt.offset(offset).limit(limit)
    return list(db.scalars(stmt).all())
```

- [ ] **Step 4: Confirmar que los tests pasan**

Run: `./.venv/Scripts/python.exe -m pytest tests/test_comprobantes.py -v -k listar_comprobantes`
Expected: 7 PASSED.

- [ ] **Step 5: Suite completa**

Run: `./.venv/Scripts/python.exe -m pytest -v 2>&1 | tail -5`
Expected: todo verde.

- [ ] **Step 6: Commit**

```bash
git add tests/test_comprobantes.py backend/routers/comprobantes.py
git commit -m "feat(comprobantes): GET /comprobantes con filtros y aislamiento por rol (TDD)"
```

---

## Fase B — Frontend: infraestructura

### Task 7: Actualizar Sidebar.jsx y App.jsx

**Files:**
- Modify: `frontend/src/components/Sidebar.jsx` (sumar 2 entradas al array MODULOS)
- Modify: `frontend/src/App.jsx` (sumar 2 routes)

- [ ] **Step 1: Editar `frontend/src/components/Sidebar.jsx`**

Buscar el array `MODULOS` y reemplazarlo por:

```jsx
const MODULOS = [
  {
    ruta: "/comunicados",
    nombre: "Comunicación",
    rolesPermitidos: ["administracion", "representante", "departamento"],
  },
  {
    ruta: "/expensas",
    nombre: "Expensas",
    rolesPermitidos: ["administracion", "departamento"],
  },
  {
    ruta: "/comprobantes",
    nombre: "Comprobantes",
    rolesPermitidos: ["administracion", "departamento"],
  },
];
```

- [ ] **Step 2: Editar `frontend/src/App.jsx`**

Sumar imports arriba con los demás imports de pantallas:

```jsx
import Expensas from "./screens/Expensas";
import Comprobantes from "./screens/Comprobantes";
```

Dentro del `<Route path="/" ...>` que envuelve el `<AppLayout />` con `<RequireAuth>`, después de la route de `comunicados`, sumar:

```jsx
<Route path="expensas" element={<Expensas />} />
<Route path="comprobantes" element={<Comprobantes />} />
```

> Nota: el build va a romper hasta que existan `Expensas.jsx` y `Comprobantes.jsx`. Esto se completa en la Task 12 y 17.

- [ ] **Step 3: Commit**

```bash
git add frontend/src/components/Sidebar.jsx frontend/src/App.jsx
git commit -m "feat(frontend): sumar Expensas y Comprobantes al sidebar y al router"
```

---

### Task 8: Crear `components/BadgeEstado.jsx`

**File:**
- Create: `frontend/src/components/BadgeEstado.jsx`

- [ ] **Step 1: Crear el archivo**

Contenido completo:

```jsx
const LABELS = {
  // Expensa
  pendiente: "Pendiente",
  pagada: "Confirmada",
  vencida: "Vencida",
  // Comprobante
  pendiente_verificacion: "Pendiente de verificación",
  aprobado: "Aprobado",
  rechazado: "Rechazado",
};

const CLASES = {
  pendiente: "badge--neutro",
  pagada: "badge--ok",
  vencida: "badge--alerta",
  pendiente_verificacion: "badge--warning",
  aprobado: "badge--ok",
  rechazado: "badge--alerta",
};

export default function BadgeEstado({ estado, vencida = false }) {
  // Si la expensa está pendiente pero ya venció, mostrar 'vencida'.
  const clave = vencida && estado === "pendiente" ? "vencida" : estado;
  return (
    <span className={`badge ${CLASES[clave] || "badge--neutro"}`}>
      {LABELS[clave] || estado}
    </span>
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add frontend/src/components/BadgeEstado.jsx
git commit -m "feat(frontend): componente BadgeEstado para expensas y comprobantes"
```

---

### Task 9: Crear `components/SelectorDepartamento.jsx`

**File:**
- Create: `frontend/src/components/SelectorDepartamento.jsx`

- [ ] **Step 1: Crear el archivo**

Contenido completo:

```jsx
import { useEffect, useState } from "react";
import { listarDepartamentos } from "../api/departamentos";

export default function SelectorDepartamento({ valor, onChange, permitirVacio = true }) {
  const [departamentos, setDepartamentos] = useState([]);
  const [cargando, setCargando] = useState(true);
  const [error, setError] = useState(null);

  useEffect(() => {
    let cancelado = false;
    async function cargar() {
      const r = await listarDepartamentos();
      if (cancelado) return;
      if (r.status === 200) {
        setDepartamentos(r.data);
        setError(null);
      } else if (r.status !== 401) {
        setError("No se pudieron cargar los departamentos.");
      }
      setCargando(false);
    }
    cargar();
    return () => {
      cancelado = true;
    };
  }, []);

  if (error) {
    return <span role="alert">{error}</span>;
  }

  return (
    <label>
      Departamento
      <select
        value={valor ?? ""}
        onChange={(e) => onChange(e.target.value === "" ? null : Number(e.target.value))}
        disabled={cargando}
      >
        {permitirVacio && <option value="">— Todos —</option>}
        {!permitirVacio && valor === null && <option value="">— Elegí uno —</option>}
        {departamentos.map((d) => (
          <option key={d.id} value={d.id}>
            {d.codigo}{d.descripcion ? ` — ${d.descripcion}` : ""}
          </option>
        ))}
      </select>
    </label>
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add frontend/src/components/SelectorDepartamento.jsx
git commit -m "feat(frontend): componente SelectorDepartamento"
```

---

### Task 10: Crear las 3 API modules

**Files:**
- Create: `frontend/src/api/departamentos.js`
- Create: `frontend/src/api/expensas.js`
- Create: `frontend/src/api/comprobantes.js`

- [ ] **Step 1: `frontend/src/api/departamentos.js`**

```js
import { apiFetch } from "./client";

export function listarDepartamentos() {
  return apiFetch("/departamentos");
}
```

- [ ] **Step 2: `frontend/src/api/expensas.js`**

```js
import { apiFetch } from "./client";

export function listarExpensas({ departamento_id, estado, periodo } = {}) {
  const params = new URLSearchParams();
  if (departamento_id != null) params.set("departamento_id", departamento_id);
  if (estado) params.set("estado", estado);
  if (periodo) params.set("periodo", periodo);
  const qs = params.toString();
  return apiFetch(`/expensas${qs ? `?${qs}` : ""}`);
}

export function obtenerExpensa(id) {
  return apiFetch(`/expensas/${id}`);
}

export function crearExpensa(payload) {
  return apiFetch("/expensas", {
    method: "POST",
    body: payload,
  });
}

export function presentarComprobante(expensa_id, payload) {
  return apiFetch(`/expensas/${expensa_id}/comprobantes`, {
    method: "POST",
    body: payload,
  });
}
```

- [ ] **Step 3: `frontend/src/api/comprobantes.js`**

```js
import { apiFetch } from "./client";

export function listarComprobantes({ estado, departamento_id } = {}) {
  const params = new URLSearchParams();
  if (estado) params.set("estado", estado);
  if (departamento_id != null) params.set("departamento_id", departamento_id);
  const qs = params.toString();
  return apiFetch(`/comprobantes${qs ? `?${qs}` : ""}`);
}

export function actualizarComprobante(id, payload) {
  return apiFetch(`/comprobantes/${id}`, {
    method: "PATCH",
    body: payload,
  });
}
```

- [ ] **Step 4: Commit**

```bash
git add frontend/src/api/departamentos.js frontend/src/api/expensas.js frontend/src/api/comprobantes.js
git commit -m "feat(frontend): api modules para departamentos, expensas y comprobantes"
```

---

## Fase C — Frontend: pantalla `/expensas`

### Task 11: `Expensas.jsx` esqueleto con GET y selector

**File:**
- Create: `frontend/src/screens/Expensas.jsx`

- [ ] **Step 1: Crear el archivo**

```jsx
import { useEffect, useState } from "react";
import { Link } from "react-router-dom";
import { useAuth } from "../auth/AuthContext";
import { listarExpensas } from "../api/expensas";
import SelectorDepartamento from "../components/SelectorDepartamento";

export default function Expensas() {
  const { user } = useAuth();
  const [expensas, setExpensas] = useState([]);
  const [cargando, setCargando] = useState(false);
  const [errorCarga, setErrorCarga] = useState(null);
  const [departamentoSeleccionado, setDepartamentoSeleccionado] = useState(null);

  const esAdmin = user.rol === "administracion";

  useEffect(() => {
    // Depto carga al montar. Admin carga al elegir un depto.
    if (esAdmin && departamentoSeleccionado === null) {
      setExpensas([]);
      return;
    }

    let cancelado = false;
    setCargando(true);

    async function cargar() {
      const params = esAdmin ? { departamento_id: departamentoSeleccionado } : {};
      const r = await listarExpensas(params);
      if (cancelado) return;
      if (r.status === 200) {
        setExpensas(r.data);
        setErrorCarga(null);
      } else if (r.status !== 401) {
        setErrorCarga("No se pudieron cargar las expensas.");
      }
      setCargando(false);
    }

    cargar();
    return () => {
      cancelado = true;
    };
  }, [esAdmin, departamentoSeleccionado]);

  return (
    <section>
      <header className="seccion-header">
        <h2>Expensas</h2>
        {esAdmin && (
          <div className="seccion-acciones">
            <SelectorDepartamento
              valor={departamentoSeleccionado}
              onChange={setDepartamentoSeleccionado}
              permitirVacio={false}
            />
          </div>
        )}
      </header>

      {esAdmin && departamentoSeleccionado === null && (
        <p>Elegí un departamento para ver sus expensas.</p>
      )}

      {cargando && <p>Cargando…</p>}
      {errorCarga && <p role="alert" className="error-banner">{errorCarga}</p>}
      {!cargando && !errorCarga && expensas.length === 0 &&
        (!esAdmin || departamentoSeleccionado !== null) && (
          <p>No hay expensas para mostrar.</p>
        )}

      <ul className="lista-expensas">
        {expensas.map((e) => (
          <li key={e.id}>
            {/* TarjetaExpensa se implementa en la próxima task */}
            <article className="tarjeta">
              <h3>{e.periodo} — ${e.monto.toLocaleString("es-AR")}</h3>
              <p className="meta">vence {e.fecha_vencimiento}</p>
              <p>Estado: {e.estado}</p>
            </article>
          </li>
        ))}
      </ul>

      <p>
        <Link to="/comprobantes">Ver todos los comprobantes →</Link>
      </p>
    </section>
  );
}
```

- [ ] **Step 2: Verificar que el build pasa**

Run (desde `frontend/`): `npx vite build 2>&1 | tail -5`
Expected: build exitoso (puede haber warnings de eslint pero no errores).

- [ ] **Step 3: Commit**

```bash
git add frontend/src/screens/Expensas.jsx
git commit -m "feat(expensas): pantalla Expensas con GET y selector de depto"
```

---

### Task 12: TarjetaExpensa con badge, leyenda y botones por estado

**File:**
- Modify: `frontend/src/screens/Expensas.jsx`

- [ ] **Step 1: Sumar imports y helpers**

En el bloque de imports al tope, agregar:

```jsx
import BadgeEstado from "../components/BadgeEstado";
import Tarjeta from "../components/Tarjeta";
```

Justo antes de `export default function Expensas()`, agregar:

```jsx
function estaVencida(expensa, hoy = new Date()) {
  if (expensa.estado !== "pendiente") return false;
  const venc = new Date(expensa.fecha_vencimiento + "T00:00:00");
  return venc < new Date(hoy.toISOString().slice(0, 10) + "T00:00:00");
}

function leyendaDeExpensa(expensa) {
  if (expensa.estado === "pagada") return null;
  const uc = expensa.ultimo_comprobante;
  if (!uc) return "Aún no presentó comprobante";
  if (uc.estado === "pendiente_verificacion") return "Comprobante pendiente de verificación";
  if (uc.estado === "rechazado") return "Último comprobante rechazado";
  return null;
}

function formatearMonto(v) {
  return v.toLocaleString("es-AR", { style: "currency", currency: "ARS", maximumFractionDigits: 0 });
}

function TarjetaExpensa({ expensa, rol, onPresentar, onConfirmar, onVer }) {
  const vencida = estaVencida(expensa);
  const leyenda = leyendaDeExpensa(expensa);
  const uc = expensa.ultimo_comprobante;
  const esAdmin = rol === "administracion";
  const esDepto = rol === "departamento";

  let boton = null;
  if (expensa.estado === "pagada") {
    boton = <button type="button" onClick={onVer}>Ver comprobante</button>;
  } else if (!uc && esDepto) {
    boton = <button type="button" onClick={onPresentar}>Presentar comprobante</button>;
  } else if (uc?.estado === "rechazado" && esDepto) {
    boton = <button type="button" onClick={onPresentar}>Presentar otro comprobante</button>;
  } else if (uc?.estado === "pendiente_verificacion" && esAdmin) {
    boton = <button type="button" onClick={onConfirmar}>Confirmar</button>;
  }

  return (
    <Tarjeta>
      <h3>{expensa.periodo} — {formatearMonto(expensa.monto)}</h3>
      <p className="meta">Vence {expensa.fecha_vencimiento}</p>
      <p>
        <BadgeEstado estado={expensa.estado} vencida={vencida} />
      </p>
      {leyenda && <p className="leyenda">ⓘ {leyenda}</p>}
      {boton && <div className="tarjeta-acciones">{boton}</div>}
    </Tarjeta>
  );
}
```

- [ ] **Step 2: Reemplazar el placeholder por TarjetaExpensa**

Buscar el bloque dentro del `.map(...)`:

```jsx
<li key={e.id}>
  {/* TarjetaExpensa se implementa en la próxima task */}
  <article className="tarjeta">
    <h3>{e.periodo} — ${e.monto.toLocaleString("es-AR")}</h3>
    <p className="meta">vence {e.fecha_vencimiento}</p>
    <p>Estado: {e.estado}</p>
  </article>
</li>
```

Reemplazarlo por:

```jsx
<li key={e.id}>
  <TarjetaExpensa
    expensa={e}
    rol={user.rol}
    onPresentar={() => {}}
    onConfirmar={() => {}}
    onVer={() => {}}
  />
</li>
```

(Los handlers se conectan a modales en las tasks siguientes.)

- [ ] **Step 3: Build**

Run (desde `frontend/`): `npx vite build 2>&1 | tail -5`
Expected: pasa.

- [ ] **Step 4: Commit**

```bash
git add frontend/src/screens/Expensas.jsx
git commit -m "feat(expensas): TarjetaExpensa con badge, leyenda y botón según estado+rol"
```

---

### Task 13: ModalNuevaExpensa (Admin)

**File:**
- Modify: `frontend/src/screens/Expensas.jsx`

- [ ] **Step 1: Sumar imports y estado**

En el bloque de imports, agregar:

```jsx
import { crearExpensa } from "../api/expensas";
import Modal from "../components/Modal";
```

Dentro de `Expensas()`, sumar al bloque de `useState`:

```jsx
  const [modalCrearAbierto, setModalCrearAbierto] = useState(false);
  const [errorAccion, setErrorAccion] = useState(null);

  function handleExpensaCreada(nueva) {
    setExpensas((prev) => [nueva, ...prev]);
    setModalCrearAbierto(false);
    setErrorAccion(null);
  }
```

- [ ] **Step 2: Agregar botón "+ Nueva expensa" en el header**

Dentro del `{esAdmin && ...}` que contiene el `SelectorDepartamento`, sumar el botón:

```jsx
{esAdmin && (
  <div className="seccion-acciones">
    <SelectorDepartamento
      valor={departamentoSeleccionado}
      onChange={setDepartamentoSeleccionado}
      permitirVacio={false}
    />
    <button
      type="button"
      disabled={departamentoSeleccionado === null}
      onClick={() => setModalCrearAbierto(true)}
    >
      + Nueva expensa
    </button>
  </div>
)}
```

- [ ] **Step 3: Mostrar errorAccion arriba de la lista**

Después de `{errorCarga && ...}`, sumar:

```jsx
{errorAccion && <p role="alert" className="error-banner">{errorAccion}</p>}
```

- [ ] **Step 4: Renderizar el modal antes del `</section>` final**

Sumar al final del JSX, antes del `</section>`:

```jsx
{modalCrearAbierto && (
  <Modal titulo="Nueva expensa" onClose={() => setModalCrearAbierto(false)}>
    <FormularioNuevaExpensa
      departamentoId={departamentoSeleccionado}
      onCreada={handleExpensaCreada}
      onCancelar={() => setModalCrearAbierto(false)}
    />
  </Modal>
)}
```

- [ ] **Step 5: Agregar el componente FormularioNuevaExpensa al final del archivo**

Después del `export default` de `Expensas`, sumar:

```jsx
function FormularioNuevaExpensa({ departamentoId, onCreada, onCancelar }) {
  const [periodo, setPeriodo] = useState("");
  const [monto, setMonto] = useState("");
  const [fechaVencimiento, setFechaVencimiento] = useState("");
  const [enviando, setEnviando] = useState(false);
  const [error, setError] = useState(null);

  async function onSubmit(e) {
    e.preventDefault();
    setError(null);
    setEnviando(true);

    const r = await crearExpensa({
      departamento_id: departamentoId,
      periodo,
      monto: Number(monto),
      fecha_vencimiento: fechaVencimiento,
    });
    setEnviando(false);

    if (r.status === 201) {
      onCreada(r.data);
      return;
    }
    if (r.status === 400) {
      setError(r.data?.detail || "Revisá los campos del formulario.");
      return;
    }
    if (r.status === 404) {
      setError("El departamento indicado no existe.");
      return;
    }
    if (r.status === 409) {
      setError(r.data?.detail || "Ya existe una expensa para ese departamento en ese período.");
      return;
    }
    if (r.status !== 401) {
      setError("Ocurrió un error inesperado. Intentá de nuevo.");
    }
  }

  return (
    <form onSubmit={onSubmit} noValidate>
      <label>
        Período (YYYY-MM)
        <input
          type="text"
          value={periodo}
          onChange={(e) => setPeriodo(e.target.value)}
          pattern="\d{4}-(0[1-9]|1[0-2])"
          placeholder="2026-06"
          required
          autoFocus
        />
      </label>
      <label>
        Monto
        <input
          type="number"
          value={monto}
          onChange={(e) => setMonto(e.target.value)}
          min="1"
          step="0.01"
          required
        />
      </label>
      <label>
        Fecha de vencimiento
        <input
          type="date"
          value={fechaVencimiento}
          onChange={(e) => setFechaVencimiento(e.target.value)}
          required
        />
      </label>

      {error && <p role="alert" className="error-banner">{error}</p>}

      <div className="modal-acciones">
        <button type="button" className="boton-secundario" onClick={onCancelar} disabled={enviando}>
          Cancelar
        </button>
        <button type="submit" disabled={enviando}>
          {enviando ? "Creando…" : "Crear expensa"}
        </button>
      </div>
    </form>
  );
}
```

- [ ] **Step 6: Build**

Run (desde `frontend/`): `npx vite build 2>&1 | tail -5`
Expected: pasa.

- [ ] **Step 7: Commit**

```bash
git add frontend/src/screens/Expensas.jsx
git commit -m "feat(expensas): modal de creación de expensa para Admin"
```

---

### Task 14: ModalPresentarComprobante (Depto)

**File:**
- Modify: `frontend/src/screens/Expensas.jsx`

- [ ] **Step 1: Sumar import de presentarComprobante**

Reemplazar el import existente `import { crearExpensa } from "../api/expensas";` por:

```jsx
import { crearExpensa, presentarComprobante } from "../api/expensas";
```

- [ ] **Step 2: Sumar estado y handler**

Dentro de `Expensas()`, junto a los otros useState:

```jsx
  const [modalPresentar, setModalPresentar] = useState(null);

  function handleComprobantePresentado(nuevoComprobante, expensaId) {
    setExpensas((prev) =>
      prev.map((e) =>
        e.id === expensaId ? { ...e, ultimo_comprobante: nuevoComprobante } : e
      )
    );
    setModalPresentar(null);
    setErrorAccion(null);
  }
```

- [ ] **Step 3: Conectar el handler `onPresentar` en TarjetaExpensa**

Dentro del `.map`, reemplazar el placeholder:

```jsx
onPresentar={() => {}}
```

por:

```jsx
onPresentar={() => setModalPresentar(e)}
```

- [ ] **Step 4: Renderizar el modal**

Después del modal de crear, sumar:

```jsx
{modalPresentar && (
  <Modal titulo="Presentar comprobante" onClose={() => setModalPresentar(null)}>
    <FormularioPresentarComprobante
      expensa={modalPresentar}
      onPresentado={(c) => handleComprobantePresentado(c, modalPresentar.id)}
      onCancelar={() => setModalPresentar(null)}
    />
  </Modal>
)}
```

- [ ] **Step 5: Agregar el componente FormularioPresentarComprobante al final del archivo**

Después de `FormularioNuevaExpensa`:

```jsx
function FormularioPresentarComprobante({ expensa, onPresentado, onCancelar }) {
  const [fechaPago, setFechaPago] = useState("");
  const [monto, setMonto] = useState(String(expensa.monto));
  const [archivoUrl, setArchivoUrl] = useState("");
  const [enviando, setEnviando] = useState(false);
  const [error, setError] = useState(null);

  async function onSubmit(e) {
    e.preventDefault();
    setError(null);
    setEnviando(true);

    const r = await presentarComprobante(expensa.id, {
      fecha_pago: fechaPago,
      monto: Number(monto),
      archivo_url: archivoUrl.trim() || null,
    });
    setEnviando(false);

    if (r.status === 201) {
      onPresentado(r.data);
      return;
    }
    if (r.status === 400) {
      setError(r.data?.detail || "Revisá los campos del formulario.");
      return;
    }
    if (r.status === 403) {
      setError("No tenés permisos para presentar este comprobante.");
      return;
    }
    if (r.status === 404) {
      setError("La expensa solicitada no existe.");
      return;
    }
    if (r.status !== 401) {
      setError("Ocurrió un error inesperado. Intentá de nuevo.");
    }
  }

  return (
    <form onSubmit={onSubmit} noValidate>
      <p className="meta">
        Expensa: {expensa.periodo} — ${expensa.monto.toLocaleString("es-AR")}
      </p>
      <label>
        Fecha de pago
        <input
          type="date"
          value={fechaPago}
          onChange={(e) => setFechaPago(e.target.value)}
          required
          autoFocus
        />
      </label>
      <label>
        Monto pagado
        <input
          type="number"
          value={monto}
          onChange={(e) => setMonto(e.target.value)}
          min="1"
          step="0.01"
          required
        />
      </label>
      <label>
        Link al comprobante (opcional)
        <input
          type="url"
          value={archivoUrl}
          onChange={(e) => setArchivoUrl(e.target.value)}
          placeholder="https://drive.google.com/..."
          maxLength={2048}
        />
      </label>

      {error && <p role="alert" className="error-banner">{error}</p>}

      <div className="modal-acciones">
        <button type="button" className="boton-secundario" onClick={onCancelar} disabled={enviando}>
          Cancelar
        </button>
        <button type="submit" disabled={enviando}>
          {enviando ? "Enviando…" : "Presentar"}
        </button>
      </div>
    </form>
  );
}
```

- [ ] **Step 6: Build**

Run (desde `frontend/`): `npx vite build 2>&1 | tail -5`
Expected: pasa.

- [ ] **Step 7: Commit**

```bash
git add frontend/src/screens/Expensas.jsx
git commit -m "feat(expensas): modal de presentar comprobante para Depto"
```

---

### Task 15: ModalConfirmar (Admin, Aprobar/Rechazar)

**File:**
- Modify: `frontend/src/screens/Expensas.jsx`

- [ ] **Step 1: Sumar import**

Agregar al tope:

```jsx
import { actualizarComprobante } from "../api/comprobantes";
```

- [ ] **Step 2: Sumar estado y handler**

Dentro de `Expensas()`, junto a los otros useState:

```jsx
  const [modalConfirmar, setModalConfirmar] = useState(null);
  const [accionando, setAccionando] = useState(false);

  async function handleDecision(comprobanteId, estadoNuevo, expensaId) {
    setAccionando(true);
    const r = await actualizarComprobante(comprobanteId, { estado: estadoNuevo });
    setAccionando(false);

    if (r.status === 200) {
      setExpensas((prev) =>
        prev.map((e) => {
          if (e.id !== expensaId) return e;
          const nuevoEstadoExpensa = estadoNuevo === "aprobado" ? "pagada" : e.estado;
          return {
            ...e,
            estado: nuevoEstadoExpensa,
            ultimo_comprobante: r.data,
          };
        })
      );
      setModalConfirmar(null);
      setErrorAccion(null);
      return;
    }
    if (r.status === 404) {
      setErrorAccion("El comprobante no existe.");
      setModalConfirmar(null);
      return;
    }
    if (r.status === 409) {
      setErrorAccion("El comprobante ya fue verificado.");
      setModalConfirmar(null);
      return;
    }
    if (r.status === 403) {
      setErrorAccion("No tenés permisos para verificar comprobantes.");
      setModalConfirmar(null);
      return;
    }
    if (r.status !== 401) {
      setErrorAccion("No se pudo verificar el comprobante. Intentá de nuevo.");
      setModalConfirmar(null);
    }
  }
```

- [ ] **Step 3: Conectar onConfirmar**

En el `.map`, reemplazar:

```jsx
onConfirmar={() => {}}
```

por:

```jsx
onConfirmar={() => setModalConfirmar({ expensa: e, comprobante: e.ultimo_comprobante })}
```

- [ ] **Step 4: Renderizar el modal**

Sumar después del modal de presentar:

```jsx
{modalConfirmar && (
  <Modal titulo="Confirmar comprobante" onClose={() => setModalConfirmar(null)}>
    <p className="meta">
      Expensa {modalConfirmar.expensa.periodo} — ${modalConfirmar.expensa.monto.toLocaleString("es-AR")}
    </p>
    <p>Pagado el {modalConfirmar.comprobante.fecha_pago}</p>
    <p>Monto: ${modalConfirmar.comprobante.monto.toLocaleString("es-AR")}</p>
    {modalConfirmar.comprobante.archivo_url && (
      <p>
        <a href={modalConfirmar.comprobante.archivo_url} target="_blank" rel="noopener noreferrer">
          Ver archivo adjunto
        </a>
      </p>
    )}
    <div className="modal-acciones">
      <button
        type="button"
        className="boton-secundario"
        onClick={() => setModalConfirmar(null)}
        disabled={accionando}
      >
        Cancelar
      </button>
      <button
        type="button"
        className="boton-peligro"
        onClick={() =>
          handleDecision(
            modalConfirmar.comprobante.id,
            "rechazado",
            modalConfirmar.expensa.id
          )
        }
        disabled={accionando}
      >
        {accionando ? "Procesando…" : "Rechazar"}
      </button>
      <button
        type="button"
        onClick={() =>
          handleDecision(
            modalConfirmar.comprobante.id,
            "aprobado",
            modalConfirmar.expensa.id
          )
        }
        disabled={accionando}
      >
        {accionando ? "Procesando…" : "Aprobar"}
      </button>
    </div>
  </Modal>
)}
```

- [ ] **Step 5: Build**

Run (desde `frontend/`): `npx vite build 2>&1 | tail -5`
Expected: pasa.

- [ ] **Step 6: Commit**

```bash
git add frontend/src/screens/Expensas.jsx
git commit -m "feat(expensas): modal de confirmar/rechazar comprobante para Admin"
```

---

### Task 16: ModalVerComprobante (ambos roles, lectura)

**File:**
- Modify: `frontend/src/screens/Expensas.jsx`

- [ ] **Step 1: Sumar estado**

```jsx
  const [modalVer, setModalVer] = useState(null);
```

- [ ] **Step 2: Conectar onVer**

En el `.map`, reemplazar:

```jsx
onVer={() => {}}
```

por:

```jsx
onVer={() => setModalVer(e.ultimo_comprobante)}
```

- [ ] **Step 3: Renderizar modal**

Después del modal de confirmar:

```jsx
{modalVer && (
  <Modal titulo="Comprobante" onClose={() => setModalVer(null)}>
    <p>Fecha de pago: <strong>{modalVer.fecha_pago}</strong></p>
    <p>Monto: <strong>${modalVer.monto.toLocaleString("es-AR")}</strong></p>
    <p>Estado: <BadgeEstado estado={modalVer.estado} /></p>
    {modalVer.archivo_url ? (
      <p>
        <a href={modalVer.archivo_url} target="_blank" rel="noopener noreferrer">
          Ver archivo adjunto
        </a>
      </p>
    ) : (
      <p className="meta">Sin archivo adjunto.</p>
    )}
    <div className="modal-acciones">
      <button type="button" onClick={() => setModalVer(null)}>Cerrar</button>
    </div>
  </Modal>
)}
```

- [ ] **Step 4: Build**

Run (desde `frontend/`): `npx vite build 2>&1 | tail -5`
Expected: pasa.

- [ ] **Step 5: Commit**

```bash
git add frontend/src/screens/Expensas.jsx
git commit -m "feat(expensas): modal de ver comprobante (lectura)"
```

---

## Fase D — Frontend: pantalla `/comprobantes`

### Task 17: `Comprobantes.jsx` con filtros y lista

**File:**
- Create: `frontend/src/screens/Comprobantes.jsx`

- [ ] **Step 1: Crear el archivo**

```jsx
import { useEffect, useState } from "react";
import { useAuth } from "../auth/AuthContext";
import { listarComprobantes } from "../api/comprobantes";
import BadgeEstado from "../components/BadgeEstado";
import SelectorDepartamento from "../components/SelectorDepartamento";
import Tarjeta from "../components/Tarjeta";

const ESTADOS = [
  { value: "", label: "Todos" },
  { value: "pendiente_verificacion", label: "Pendientes" },
  { value: "aprobado", label: "Aprobados" },
  { value: "rechazado", label: "Rechazados" },
];

export default function Comprobantes() {
  const { user } = useAuth();
  const esAdmin = user.rol === "administracion";

  const [comprobantes, setComprobantes] = useState([]);
  const [cargando, setCargando] = useState(true);
  const [errorCarga, setErrorCarga] = useState(null);
  const [filtroEstado, setFiltroEstado] = useState("");
  const [filtroDepto, setFiltroDepto] = useState(null);

  useEffect(() => {
    let cancelado = false;
    setCargando(true);

    async function cargar() {
      const params = {};
      if (filtroEstado) params.estado = filtroEstado;
      if (esAdmin && filtroDepto != null) params.departamento_id = filtroDepto;

      const r = await listarComprobantes(params);
      if (cancelado) return;

      if (r.status === 200) {
        setComprobantes(r.data);
        setErrorCarga(null);
      } else if (r.status !== 401) {
        setErrorCarga("No se pudieron cargar los comprobantes.");
      }
      setCargando(false);
    }

    cargar();
    return () => {
      cancelado = true;
    };
  }, [filtroEstado, filtroDepto, esAdmin]);

  return (
    <section>
      <header className="seccion-header">
        <h2>Comprobantes</h2>
      </header>

      <div className="filtros">
        <label>
          Estado
          <select value={filtroEstado} onChange={(e) => setFiltroEstado(e.target.value)}>
            {ESTADOS.map((o) => (
              <option key={o.value} value={o.value}>{o.label}</option>
            ))}
          </select>
        </label>
        {esAdmin && (
          <SelectorDepartamento valor={filtroDepto} onChange={setFiltroDepto} permitirVacio />
        )}
      </div>

      {cargando && <p>Cargando…</p>}
      {errorCarga && <p role="alert" className="error-banner">{errorCarga}</p>}
      {!cargando && !errorCarga && comprobantes.length === 0 && (
        <p>No hay comprobantes con esos filtros.</p>
      )}

      <ul className="lista-comprobantes">
        {comprobantes.map((c) => (
          <li key={c.id}>
            <Tarjeta>
              <h3>
                {c.expensa?.periodo ?? "—"} · ${c.monto.toLocaleString("es-AR")}
              </h3>
              <p className="meta">Pagado {c.fecha_pago}</p>
              {c.expensa && (
                <p className="meta">Departamento: {c.expensa.departamento_id}</p>
              )}
              <p><BadgeEstado estado={c.estado} /></p>
              {c.archivo_url && (
                <p>
                  <a href={c.archivo_url} target="_blank" rel="noopener noreferrer">
                    Ver archivo
                  </a>
                </p>
              )}
              {/* Botones Aprobar/Rechazar para Admin se agregan en la próxima task */}
            </Tarjeta>
          </li>
        ))}
      </ul>
    </section>
  );
}
```

- [ ] **Step 2: Build**

Run (desde `frontend/`): `npx vite build 2>&1 | tail -5`
Expected: pasa.

- [ ] **Step 3: Commit**

```bash
git add frontend/src/screens/Comprobantes.jsx
git commit -m "feat(comprobantes): pantalla con filtros y lista de comprobantes"
```

---

### Task 18: Aprobar / Rechazar inline para Admin

**File:**
- Modify: `frontend/src/screens/Comprobantes.jsx`

- [ ] **Step 1: Sumar import de actualizarComprobante**

Reemplazar el import existente:

```jsx
import { listarComprobantes, actualizarComprobante } from "../api/comprobantes";
```

- [ ] **Step 2: Sumar estado y handler**

Dentro de `Comprobantes()`, junto a los otros useState:

```jsx
  const [accionandoId, setAccionandoId] = useState(null);
  const [errorAccion, setErrorAccion] = useState(null);

  async function handleDecision(id, estadoNuevo) {
    setAccionandoId(id);
    const r = await actualizarComprobante(id, { estado: estadoNuevo });
    setAccionandoId(null);

    if (r.status === 200) {
      setComprobantes((prev) => prev.map((c) => (c.id === id ? r.data : c)));
      setErrorAccion(null);
      return;
    }
    if (r.status === 404) {
      setComprobantes((prev) => prev.filter((c) => c.id !== id));
      setErrorAccion("El comprobante no existe.");
      return;
    }
    if (r.status === 409) {
      setErrorAccion("El comprobante ya fue verificado.");
      return;
    }
    if (r.status === 403) {
      setErrorAccion("No tenés permisos para esta operación.");
      return;
    }
    if (r.status !== 401) {
      setErrorAccion("Ocurrió un error. Intentá de nuevo.");
    }
  }
```

- [ ] **Step 3: Mostrar errorAccion arriba de la lista**

Después del `errorCarga`:

```jsx
{errorAccion && <p role="alert" className="error-banner">{errorAccion}</p>}
```

- [ ] **Step 4: Agregar botones Aprobar/Rechazar en la tarjeta**

Dentro de la tarjeta del `.map`, justo antes del cierre `</Tarjeta>`, reemplazar el comentario:

```jsx
{/* Botones Aprobar/Rechazar para Admin se agregan en la próxima task */}
```

por:

```jsx
{esAdmin && c.estado === "pendiente_verificacion" && (
  <div className="tarjeta-acciones">
    <button
      type="button"
      onClick={() => handleDecision(c.id, "aprobado")}
      disabled={accionandoId === c.id}
    >
      {accionandoId === c.id ? "…" : "Aprobar"}
    </button>
    <button
      type="button"
      className="boton-borrar"
      onClick={() => handleDecision(c.id, "rechazado")}
      disabled={accionandoId === c.id}
    >
      {accionandoId === c.id ? "…" : "Rechazar"}
    </button>
  </div>
)}
```

- [ ] **Step 5: Build**

Run (desde `frontend/`): `npx vite build 2>&1 | tail -5`
Expected: pasa.

- [ ] **Step 6: Commit**

```bash
git add frontend/src/screens/Comprobantes.jsx
git commit -m "feat(comprobantes): aprobar/rechazar inline para Admin"
```

---

## Fase E — CSS + verificación

### Task 19: CSS para badges, layouts y filtros (mobile-first)

**File:**
- Modify: `frontend/src/index.css`

- [ ] **Step 1: Sumar variables de warning al `:root`**

Buscar el bloque `:root { ... }` en `index.css`. Sumar antes del cierre `}`:

```css
  --color-warning: #d97706;
  --color-warning-bg: #fef3c7;
```

- [ ] **Step 2: Sumar estilos para badges, filtros y listas al final del archivo**

Antes del bloque `@media (min-width: 600px)` que ya existe, agregar:

```css
/* ---------- Badges ---------- */

.badge {
  display: inline-block;
  padding: 0.2em 0.6em;
  border-radius: 999px;
  border: 1px solid var(--color-border);
  font-size: 0.8rem;
  background: var(--color-surface);
}

.badge--neutro {
  color: var(--color-text-muted);
  border-color: var(--color-border);
}

.badge--ok {
  color: var(--color-success);
  border-color: var(--color-success);
}

.badge--alerta {
  color: var(--color-danger);
  border-color: var(--color-danger);
  background: var(--color-danger-bg);
}

.badge--warning {
  color: var(--color-warning);
  border-color: var(--color-warning);
  background: var(--color-warning-bg);
}

/* ---------- Leyenda ---------- */

.leyenda {
  color: var(--color-text-muted);
  font-size: 0.85rem;
  margin: 0.25rem 0 0.5rem;
}

/* ---------- Filtros ---------- */

.filtros {
  display: flex;
  flex-direction: column;
  gap: 0.75rem;
  margin-bottom: 1rem;
}

.filtros label {
  font-size: 0.9rem;
}

.filtros select {
  font-family: inherit;
  font-size: 1rem;
  padding: 0.55em 0.75em;
  min-height: 44px;
  border: 1px solid var(--color-border);
  border-radius: var(--radius);
  background: var(--color-surface);
  color: var(--color-text);
  width: 100%;
}

/* ---------- Acciones del header (selector + botón) ---------- */

.seccion-acciones {
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
  align-items: stretch;
}

/* ---------- Listas de expensas y comprobantes ---------- */

.lista-expensas,
.lista-comprobantes {
  list-style: none;
  margin: 0;
  padding: 0;
  display: flex;
  flex-direction: column;
  gap: 1rem;
}
```

- [ ] **Step 3: Sumar mejoras desktop en los media queries**

Dentro del bloque `@media (min-width: 600px) { ... }` existente, antes del cierre `}`, sumar:

```css
  .filtros {
    flex-direction: row;
    align-items: flex-end;
  }

  .seccion-acciones {
    flex-direction: row;
    align-items: center;
    gap: 0.75rem;
  }
```

- [ ] **Step 4: Build**

Run (desde `frontend/`): `npx vite build 2>&1 | tail -5`
Expected: pasa.

- [ ] **Step 5: Commit**

```bash
git add frontend/src/index.css
git commit -m "style(frontend): badges, filtros, listas y seccion-acciones (mobile-first)"
```

---

### Task 20: Smoke test end-to-end

- [ ] **Step 1: Tests backend completos**

Run: `./.venv/Scripts/python.exe -m pytest -v 2>&1 | tail -5`
Expected: toda la suite verde.

- [ ] **Step 2: Verificación visual como Admin**

Con el backend corriendo (`uvicorn backend.main:app --reload`) y el frontend (`npm run dev` desde `frontend/`):

1. Logout si quedaste logueado.
2. Login como Admin (`admin@test.local` / `test-pass-1234`).
3. Sidebar muestra "Comunicación", "Expensas", "Comprobantes".
4. Click "Expensas" → pantalla con selector de depto vacío y mensaje "Elegí un departamento…".
5. Elegir un depto → carga sus expensas. Aparece botón "+ Nueva expensa".
6. Crear una expensa nueva (período del mes siguiente, monto, vencimiento). Aparece en la lista con badge "Pendiente" y leyenda "Aún no presentó comprobante".
7. Click en "Comprobantes" → vista con filtros. Filtrar por "Pendientes" — vacío si no se cargaron comprobantes todavía.

- [ ] **Step 3: Verificación visual como Departamento**

1. Logout, login como Depto A (`a@test.local` / `test-pass-1234`).
2. Sidebar muestra "Comunicación", "Expensas", "Comprobantes" (NO ve los comprobantes ni la creación).
3. Click "Expensas" → carga directo sin selector. Lista las expensas del depto.
4. En una expensa pendiente, "Presentar comprobante" → modal con fecha de pago, monto, link opcional. Al enviar, la tarjeta refresca a "Comprobante pendiente de verificación".
5. Click "Comprobantes" → ve los comprobantes propios. Sin botones Aprobar/Rechazar.

- [ ] **Step 4: Vuelta a Admin para aprobar**

1. Logout, login como Admin.
2. Click "Comprobantes". Filtrar por "Pendientes". Aparece el que presentó el depto.
3. Click "Aprobar" → la tarjeta refresca a estado "Aprobado".
4. Click "Expensas" → elegir el mismo depto. La expensa figura como "Confirmada" + botón "Ver comprobante".
5. Click "Ver comprobante" → modal con info del comprobante aprobado.

- [ ] **Step 5: Verificación como Representante**

1. Logout, login como Repre (`repre@test.local` / `test-pass-1234`).
2. Sidebar muestra solo "Comunicación". No ve "Expensas" ni "Comprobantes".
3. Si manualmente navega a `/expensas` o `/comprobantes`, el backend devuelve 403 y la pantalla muestra el error banner.

- [ ] **Step 6: Listo**

Si todos los chequeos pasan, el módulo está completo. Mergear `feature/expensas-frontend` a `master` está fuera del scope de este plan (lo decide el usuario).
